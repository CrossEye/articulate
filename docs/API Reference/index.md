API Reference
=============

All endpoints are prefixed with `/api/v1`.  Responses are JSON.  Authentication is via session cookies.

*Endpoint documentation will be added as each API route is implemented.*
